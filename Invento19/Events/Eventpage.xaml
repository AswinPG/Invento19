<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:forms="clr-namespace:FFImageLoading.Svg.Forms;assembly=FFImageLoading.Svg.Forms"
             mc:Ignorable="d"
             x:Class="Invento19.Events.Eventpage">
   


    <ContentPage.Content>
       
        <Grid VerticalOptions="FillAndExpand"
              BackgroundColor="White">
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>
            </Grid.ColumnDefinitions>
            <StackLayout 
                BackgroundColor="White"
                Orientation="Horizontal">
                <forms:SvgCachedImage Source="path.svg"
                                          HorizontalOptions="Start" 
                                          Margin="10"
                                          HeightRequest="15"/>
                <StackLayout Orientation="Vertical"
                                 VerticalOptions="Center"
                                 >
                    <Label Text="Events"
                           TextColor="#5D00EF"
                           FontAttributes="Bold"
                           FontSize="Large"
                          
                           />
                </StackLayout>

            </StackLayout>
            <ScrollView Grid.Row="1" >
            <Grid VerticalOptions="Start" 
              >
               <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto"/>

                </Grid.ColumnDefinitions>
               <Grid.RowDefinitions>
                
                <RowDefinition Height="30"/>
                <RowDefinition Height="70" />
                <RowDefinition Height="30"/>
                <RowDefinition Height="70"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="500"/>
                <RowDefinition Height="30"/>
                <RowDefinition Height="150"/>
               </Grid.RowDefinitions>
           
                  
            <Grid Grid.Row="0"
                        Grid.Column="0"
                        HorizontalOptions="Start"
                           Margin="10,0,0,0"     >
                <forms:SvgCachedImage Source="Workshop.svg"
                                         
                                         Aspect="AspectFit"  />
            </Grid>

            <CollectionView  Grid.Row="1"
                        Grid.Column="0"
                            x:Name="FirstCollectionView"
                             VerticalOptions="FillAndExpand"
                        
                            >
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Horizontal"/>
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <StackLayout Orientation="Horizontal">
                            <forms:SvgCachedImage Source="{Binding .}" Margin="10" />
                        </StackLayout>
                    </DataTemplate>
                    </CollectionView.ItemTemplate>
                </CollectionView>
            <Grid Grid.Row="2"
                         
                         VerticalOptions="FillAndExpand"
                        HorizontalOptions="Start"
                        Margin="10,0,0,0">
                <forms:SvgCachedImage Source="Talks.svg"
                                      Aspect="AspectFit"
                                      />
            </Grid>
            <CollectionView  Grid.Row="3"
                             VerticalOptions="FillAndExpand"
                       
                        Grid.Column="0"
                            x:Name="SecondCollectionView">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Horizontal"/>
                </CollectionView.ItemsLayout>
                    
                <CollectionView.ItemTemplate>
                    <DataTemplate >

                        <StackLayout HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"
                                     Orientation="Horizontal">
                            <forms:SvgCachedImage
                                Source="{Binding .}" Margin="10" >
                            </forms:SvgCachedImage>
                            
                        </StackLayout>


                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <Grid Grid.Row="4"
                        Grid.Column="0"
                          VerticalOptions="FillAndExpand"
                         HorizontalOptions="Start" 
                         Margin="10,0,0,0"   >
                <forms:SvgCachedImage Source="Competition.svg"
                                         
                                       Aspect="AspectFit"
                                       />
            </Grid>
            <CollectionView Grid.Row="5"
                            x:Name="MainCollectionView"
                        Grid.Column="0">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical"
                                     Span="3" 
                                     />
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>

                        <StackLayout Orientation="Vertical" HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand"
                                    >
                            <forms:SvgCachedImage Source="{Binding .}" Margin="10" HeightRequest="100"  />
                            <StackLayout Orientation="Vertical" HorizontalOptions="Center"
                                         Margin="0,-15,0,10">
                            <Label 
                               Text="{Binding .}"
                               FontAttributes="Bold"
                                TextColor="Black"
                                />
                            <Label 
                               Text="{Binding .}"
                               
                                TextColor="Black"
                               Margin="0,-10,0,0" />
                            </StackLayout>
                        </StackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
                    <Grid Grid.Row="6"
                        Grid.Column="0"
                          VerticalOptions="FillAndExpand"
                         HorizontalOptions="Start" 
                         Margin="10,0,0,0"   >
                        <forms:SvgCachedImage Source="Competition.svg"
                                         
                                       Aspect="AspectFit"
                                       />
                    </Grid>
                    <CollectionView Grid.Row="7"
                            x:Name="CompCollectionView"
                                    VerticalOptions="FillAndExpand"
                        Grid.Column="0">
                        <CollectionView.ItemsLayout>
                            <GridItemsLayout Orientation="Horizontal"/>
                        </CollectionView.ItemsLayout>

                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <StackLayout Orientation="Horizontal" >
                                    <StackLayout Orientation="Vertical" >
                                        <forms:SvgCachedImage Source="{Binding .}" Margin="10" HeightRequest="100" WidthRequest="100"  />
                                        <Label HorizontalOptions="Center"
                               Text="{Binding .}"
                               FontAttributes="Bold"
                                TextColor="Black"
                                  Margin="0,-10,0,0"             />
                                        <Label HorizontalOptions="Center"
                               Text="{Binding .}"
                               
                                TextColor="Black"
                               Margin="0,-10,0,0" />
                                    </StackLayout>
                                </StackLayout>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                     </CollectionView>

                </Grid>
            </ScrollView>
        </Grid>
    </ContentPage.Content>
</ContentPage>